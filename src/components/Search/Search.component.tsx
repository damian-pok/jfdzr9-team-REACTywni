import { SearchWrapper } from "./Search.styled";
import { SearchEngine } from "../SearchEngine/SearchEngine.component";

export const Search = () => {
  return (
    <SearchWrapper>
      <SearchEngine />
    </SearchWrapper>
  );
};
